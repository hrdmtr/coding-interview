# PostgreSQL データベース設定
# Docker Compose 使用時はこれらの環境変数は不要です（docker-compose.yml で設定済み）
# Docker を使わない場合や、検証環境・本番環境では適切な方法で設定してください

POSTGRES_DB=coding-test
POSTGRES_USER=root
POSTGRES_PASSWORD=password
DB_HOST_NAME=localhost
DB_PORT=5432
